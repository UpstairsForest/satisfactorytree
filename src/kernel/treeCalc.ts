import {OutputNode} from "./models";

export function treeCalc(desiredOutput: Array<OutputNode>): Array<Array<OutputNode>> {
    const outArr = [];
    // take input (name and rate)
    // load recipes

    // fill in first row

    // while things have inputs:
        // fill in next row based on inputs and recipes
        // merge duplicates from this and previous row into this

    return outArr;
}